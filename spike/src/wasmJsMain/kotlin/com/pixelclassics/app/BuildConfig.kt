package com.pixelclassics.app

/** Arcade build flags — the hall always exposes the full shelf; access is
 *  governed by quarters at the cabinet door, not by an edition split. */
object BuildConfig {
    const val FREE_EDITION: Boolean = false
}
